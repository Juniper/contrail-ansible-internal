---
- name: configure agent nodemanager collector
  ini_file: dest={{ nodemanager_conf }} section=COLLECTOR option=server_list value={{ analytics_collectors_with_port }} create=yes
  notify: restart vrouter nodemanager
