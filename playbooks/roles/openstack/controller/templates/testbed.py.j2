from fabric.api import env

#Management ip addresses of hosts in the cluster
host1 = 'root@{{groups["openstack-controllers"][0]}}'


#Host from which the fab commands are triggered to install and provision
host_build = 'root@{{groups["openstack-controllers"][0]}}'


#Role definition of the hosts.
env.roledefs = {
    'all': [host1],
    'openstack': [host1],
    'cfgm': [host1],
    'build': [host1],
}

env.hostnames = {
    host1: '{{ hostvars[groups["openstack-controllers"][0]]["ansible_hostname"] }}',
}

# Passwords of each host
# for passwordless login's no need to set env.passwords,
# instead populate env.key_filename in testbed.py with public key.
env.passwords = {
    host1: 'c0ntrail123',
    host_build: 'c0ntrail123',
}
